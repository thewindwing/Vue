老师讲讲验证码机制吧，他是从后台生成的图片吧，将随机生成的字符进行混乱，传到前端的，等用户输入在从表单中传回后端验证吧，还是在前端就验证啦
首先：验证码是从后端生成的，随机生成的；【后端永远认为前端有可能会被伪造】
  1. 后端调用相关的绘图第三方类库，或是（平台PHP、.NET、Java）系统核心绘图类库进行图片的绘制；
  2. 绘制的那些随机的数字、字母，都是后端预先定义好的；
  3. 将绘制的图片的URL地址，通过网络返送给客户端，然后，客户端可以使用img标签，去引用这个验证码的地址；
  4. 后端在绘制完毕验证码之后，注意，随机选择生成的字母，不能丢弃，而是需要保存到 Session 中；
  5. 当客户端输入验证码完毕后，会提交表单，后端服务器会拿到 客户端提交过来的 验证码，与 服务器端的Session中的验证码进行比较；

老师数据库在工作中重要吗，我几乎放弃了呢....
【对于前端来说，不会数据库，照样是个好前端】
【但是，对于后端来说，如果不会数据库，那就不是个真正的后端】

server.on('request', function (req,res) {如何在页面打印出来req }
【res.end(req)   end:  字符串 和 Buffer】
【res.end(JSON.stringify(req))】